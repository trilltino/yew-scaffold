[build]
target = "index.html"
public_url = "/yew-scaffold/"
wasm_opt = false   # skip wasm-opt so it doesn’t crash on Windows

[serve]
open = false
spa = false        # we’ll handle SPA fallback manually (404.html)

[[copy]]
source = "static"
dest = "static"
